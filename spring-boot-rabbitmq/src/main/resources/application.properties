#主配置文件，配置了这个会优先读取里面的属性覆盖主配置文件的属性
spring.profiles.active=dev

# 应用自定义配置
logging.config=classpath:logback-seanchen.xml

# 最大支持文件大小
spring.http.multipart.max-file-size=2Mb
# 最大支持请求大小
spring.http.multipart.max-request-size=10Mb